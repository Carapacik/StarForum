@model SForum.Models.ApplicationUser.ProfileListModel
@{
    ViewData["Title"] = "Пользователи";
}

<div class="container body-content">
    <div class="row sectionHeader">
        <div class="sectionHeading">Пользователи</div>
    </div>
    <div class="row" id="forumIndexContent">
        <table class="table table-hover" id="userIndexTable">
            <thead>
            <tr>
                <th>
                    Изображение
                </th>
                <th>
                    <a asp-controller="Profile" asp-action="Index" asp-route-typeQuery="userName" class="linkDecorationNone">
                        Имя
                        @if (Model.TypeQuery == "userName")
                        {
                            <i class="material-icons">arrow_drop_down</i>
                        }
                    </a>
                </th>
                <th>
                    E-mail
                </th>
                <th>
                    <a asp-controller="Profile" asp-action="Index" asp-route-typeQuery="memberSince" class="linkDecorationNone">
                        Дата регистрации
                        @if (Model.TypeQuery == "memberSince")
                        {
                            <i class="material-icons">arrow_drop_down</i>
                        }
                    </a>
                </th>
                <th>
                    <a asp-controller="Profile" asp-action="Index" asp-route-typeQuery="" class="linkDecorationNone">
                        Рейтинг
                        @if (Model.TypeQuery == "rating")
                        {
                            <i class="material-icons">arrow_drop_down</i>
                        }
                    </a>
                </th>
            </tr>
            </thead>
            <tbody>
            @foreach (var profile in Model.Profiles)
            {
                <tr class="userRow">
                    <td>
                        <div class="userProfilePic" style="background-image: url(@profile.ProfileImageUrl)"></div>
                    </td>
                    <td>
                        <a asp-controller="Profile" asp-action="Detail" asp-route-id="@profile.UserId">
                            @profile.Username
                        </a>
                    </td>
                    <td>
                        @profile.Email
                    </td>
                    <td>
                        @profile.MemberSince
                    </td>
                    <td>
                        @profile.UserRating
                    </td>
                </tr>
            }
            </tbody>
        </table>
    </div>
</div>